<div class="mx-auto md:w-2/3 w-full flex flex-col items-center gap-y-5">
  <h1 class="font-bold font-bold text-white text-4xl font-outline-shadow text-center"> <%= t('.title') %> </h1>

  <%= render "form", user: @user %>

  <%= link_to "Cancel", root_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
